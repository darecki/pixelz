# Copy to .env.local and fill in. Use .env.local for local dev (API and Vite load from repo root).
# Do NOT run "vercel env pull" into .env.local – it overwrites this file. Add vars in Vercel dashboard instead.
#
# --- Supabase (Project Settings → API) ---
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_PUBLISHABLE_KEY=your_publishable_key_from_supabase_dashboard
SUPABASE_SECRET_KEY=your_secret_key_from_supabase_dashboard
# API verifies JWTs via Supabase JWKS (no JWT secret needed).
#
# --- Web app (Vite; only VITE_* are exposed to the client) ---
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_publishable_key_from_supabase_dashboard
# Local dev: use local API. Run "pnpm dev:api" so the app at :5173 can reach :3000.
# Production: set this to your deployed API URL (e.g. https://your-api.vercel.app) in Vercel env.
VITE_API_URL=http://localhost:3000
#
# --- Database: use POOLER URI, not Direct connection ---
# Supabase: Project Settings → Database → Connect → "Connection pooling" → Transaction (or Session) → URI.
# Direct (db.xxx.supabase.co) can cause getaddrinfo ENOTFOUND; pooler (xxx.pooler.supabase.com) works.
# Replace [YOUR-PASSWORD] with your database password.
DATABASE_URL=postgresql://postgres.[ref]:[YOUR-PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres
