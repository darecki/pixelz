# Pixelz OpenAPI

This package holds the checked-in HTTP contract starter for the shared backend.

The initial scope mirrors the endpoints used by the first React Native slice:

- leaderboards
- boards
- anonymous registration
- anonymous merge
- sync
- session invite lookup
- session detail and session actions

The file is intentionally lightweight for now and should evolve alongside backend behavior.
